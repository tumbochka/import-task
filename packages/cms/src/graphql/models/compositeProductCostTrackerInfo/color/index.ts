import { TenantResolverConfigFactory } from '../../../helpers/TenantResolverConfigFactory';

export const colorResolversConfig = new TenantResolverConfigFactory(
  'invtCmpColor',
).buildResolversConfig();
