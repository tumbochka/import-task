query productAttributeOptions(
  $filters: ProductAttributeOptionFiltersInput
  $sort: [String]
) {
  productAttributeOptions(
    filters: $filters
    pagination: { limit: -1 }
    sort: $sort
  ) {
    data {
      ...ProductAttributeOption
    }
  }
}
