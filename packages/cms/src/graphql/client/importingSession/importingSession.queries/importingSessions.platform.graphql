query importingSessions(
  $filters: ImportingSessionFiltersInput
  $pagination: PaginationArg
  $sort: [String]
) {
  importingSessions(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...ImportingSession
    }
    meta {
      ...Meta
    }
  }
}
