fragment UserMin on UsersPermissionsUserEntity {
  id
  attributes {
    fullName
    email
    jobTitle
    defaultRoute
    defaultPaymentTerminalId
    isGoogleAddressInputEnabled
    avatar {
      data {
        ...File
      }
    }
    preferredCurrency
  }
}
