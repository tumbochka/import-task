query unreadNotificationCount {
  userNotifications(
    filters: { hasBeenSeen: { eq: false } }
    pagination: { limit: 100 }
  ) {
    data {
      id
    }
    meta {
      pagination {
        total
      }
    }
  }
}
