mutation register($input: UsersPermissionsRegisterInput!) {
  register(input: $input) {
    user {
      id
      username
      email
      confirmed
      blocked
      role {
        id
        name
        description
        type
      }
    }
  }
}
