mutation registerCustomer($input: CustomerUserRegisterInput!) {
  registerCustomer(input: $input) {
    jwt
    user {
      id
      username
      email
      confirmed
      blocked
      role {
        id
        name
        description
        type
      }
    }
  }
}
