query metalTypes(
  $filters: MetalTypeFiltersInput
  $pagination: PaginationArg = {}
  $sort: [String] = []
) {
  metalTypes(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...MetalType
    }
    meta {
      ...Meta
    }
  }
}
