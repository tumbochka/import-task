query designStyles(
  $filters: DesignStyleFiltersInput
  $pagination: PaginationArg = {}
  $sort: [String] = []
) {
  designStyles(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...DesignStyle
    }
    meta {
      ...Meta
    }
  }
}
