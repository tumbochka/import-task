query engravingTypes(
  $filters: EngravingTypeFiltersInput
  $pagination: PaginationArg = {}
  $sort: [String] = []
) {
  engravingTypes(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...EngravingType
    }
    meta {
      ...Meta
    }
  }
}
