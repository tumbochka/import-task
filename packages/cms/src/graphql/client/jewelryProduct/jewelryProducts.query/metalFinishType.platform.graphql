query metalFinishTypes(
  $filters: MetalFinishTypeFiltersInput
  $pagination: PaginationArg = {}
  $sort: [String] = []
) {
  metalFinishTypes(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...MetalFinishType
    }
    meta {
      ...Meta
    }
  }
}
