query plattingTypes(
  $filters: PlattingTypeFiltersInput
  $pagination: PaginationArg = {}
  $sort: [String] = []
) {
  plattingTypes(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...PlattingType
    }
    meta {
      ...Meta
    }
  }
}
