query jewelryGenderTypes(
  $filters: GenderTypeFiltersInput
  $pagination: PaginationArg = {}
  $sort: [String] = []
) {
  genderTypes(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...JewelryGenderType
    }
    meta {
      ...Meta
    }
  }
}
