query backings(
  $filters: BackingFiltersInput
  $pagination: PaginationArg = {}
  $sort: [String] = []
) {
  backings(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...Backing
    }
    meta {
      ...Meta
    }
  }
}
