query allJewelryProducts(
  $filters: JewelryProductFiltersInput
  $pagination: PaginationArg = {}
  $sort: [String] = []
) {
  jewelryProducts(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...JewelryProduct
    }
    meta {
      ...Meta
    }
  }
}
