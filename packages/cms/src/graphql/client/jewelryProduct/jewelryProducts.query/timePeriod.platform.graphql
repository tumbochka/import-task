query timePeriods(
  $filters: TimePeriodFiltersInput
  $pagination: PaginationArg = {}
  $sort: [String] = []
) {
  timePeriods(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...TimePeriod
    }
    meta {
      ...Meta
    }
  }
}
