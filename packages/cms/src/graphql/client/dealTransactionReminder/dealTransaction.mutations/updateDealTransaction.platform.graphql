mutation updateDealTransactionReminder(
  $id: ID!
  $input: DealTransactionReminderInput!
) {
  updateDealTransactionReminder(id: $id, data: $input) {
    data {
      ...DealTransactionReminder
    }
  }
}
