query productsSetting(
  $filters: ProductSettingFiltersInput
  $pagination: PaginationArg
  $sort: [String]
) {
  productsSetting(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...ProductSetting
    }
    meta {
      ...Meta
    }
  }
}
