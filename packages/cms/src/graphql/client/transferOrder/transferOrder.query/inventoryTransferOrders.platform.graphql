query inventoryTransferOrders(
  $filters: TransferOrderFiltersInput
  $pagination: PaginationArg = {}
  $sort: [String] = []
) {
  transferOrders(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...InventoryTransferOrder
    }
    meta {
      ...Meta
    }
  }
}
