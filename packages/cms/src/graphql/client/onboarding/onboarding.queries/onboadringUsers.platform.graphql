query onboardingUsers(
  $filters: OnboardingUserFiltersInput!
  $pagination: PaginationArg! = {}
  $sort: [String] = []
) {
  onboardingUsers(sort: $sort, filters: $filters, pagination: $pagination) {
    data {
      ...OnboardingUser
    }
  }
}
