query productInventoryItems(
  $filters: ProductInventoryItemFiltersInput
  $pagination: PaginationArg = {}
  $sort: [String] = []
) {
  productInventoryItems(
    filters: $filters
    pagination: $pagination
    sort: $sort
  ) {
    data {
      ...ProductInventoryItem
    }
    meta {
      ...Meta
    }
  }
}
