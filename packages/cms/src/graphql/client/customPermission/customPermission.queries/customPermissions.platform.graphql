query customPermissions(
  $filters: CustomPermissionFiltersInput
  $pagination: PaginationArg
  $sort: [String]
) {
  customPermissions(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...CustomPermission
    }
    meta {
      ...Meta
    }
  }
}
