query serviceSetting(
  $filters: ServiceSettingFiltersInput
  $pagination: PaginationArg
  $sort: [String]
) {
  servicesSetting(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...ServiceSetting
    }
  }
}
