query invtCmpAttrOpts(
  $filters: InvtCmpAttrOptFiltersInput
  $pagination: PaginationArg
  $sort: [String]
) {
  invtCmpAttrOpts(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...InvtCmpAttrOpt
    }
    meta {
      ...Meta
    }
  }
}
