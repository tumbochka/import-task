query inventoryResourcesTable(
  $filters: ResourceFiltersInput
  $pagination: PaginationArg = {}
  $sort: [String] = []
) {
  resources(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...InventoryResourceTable
    }
    meta {
      ...Meta
    }
  }
}
