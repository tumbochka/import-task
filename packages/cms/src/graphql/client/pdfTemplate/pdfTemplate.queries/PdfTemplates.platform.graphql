query pdfTemplates(
  $filters: PdfTemplateFiltersInput
  $pagination: PaginationArg
  $sort: [String]
) {
  pdfTemplates(filters: $filters, pagination: $pagination, sort: $sort) {
    data {
      ...PdfTemplate
    }
    meta {
      ...Meta
    }
  }
}
