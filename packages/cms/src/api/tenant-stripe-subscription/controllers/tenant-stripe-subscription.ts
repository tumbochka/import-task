/**
 * tenant-stripe-subscription controller
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController(
  'api::tenant-stripe-subscription.tenant-stripe-subscription',
);
