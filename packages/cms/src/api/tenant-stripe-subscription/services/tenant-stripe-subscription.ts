/**
 * tenant-stripe-subscription service
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreService(
  'api::tenant-stripe-subscription.tenant-stripe-subscription',
);
