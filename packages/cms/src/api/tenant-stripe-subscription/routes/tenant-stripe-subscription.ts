/**
 * tenant-stripe-subscription router
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter(
  'api::tenant-stripe-subscription.tenant-stripe-subscription',
);
