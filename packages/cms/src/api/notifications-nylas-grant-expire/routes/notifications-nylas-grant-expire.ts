/**
 * notifications-nylas-grant-expire router
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter(
  'api::notifications-nylas-grant-expire.notifications-nylas-grant-expire',
);
