/**
 * enrolled-contact-condition service
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreService(
  'api::enrolled-contact-condition.enrolled-contact-condition',
);
