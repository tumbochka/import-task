/**
 * enrolled-contact-condition router
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter(
  'api::enrolled-contact-condition.enrolled-contact-condition',
);
