/**
 * enrolled-contact-condition controller
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController(
  'api::enrolled-contact-condition.enrolled-contact-condition',
);
