/**
 * maintenance-quantity-notification controller
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController(
  'api::maintenance-quantity-notification.maintenance-quantity-notification',
);
