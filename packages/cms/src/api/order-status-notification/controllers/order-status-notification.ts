/**
 * order-status-notification controller
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController(
  'api::order-status-notification.order-status-notification',
);
