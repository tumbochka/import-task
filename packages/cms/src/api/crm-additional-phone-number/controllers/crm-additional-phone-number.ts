/**
 * crm-additional-phone-number controller
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController(
  'api::crm-additional-phone-number.crm-additional-phone-number',
);
