/**
 * crm-additional-phone-number router
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter(
  'api::crm-additional-phone-number.crm-additional-phone-number',
);
