/**
 * crm-additional-phone-number service
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreService(
  'api::crm-additional-phone-number.crm-additional-phone-number',
);
