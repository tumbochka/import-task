/**
 * deal-transaction-reminder controller
 */
import { factories } from '@strapi/strapi';

export default factories.createCoreController(
  'api::deal-transaction-reminder.deal-transaction-reminder',
);
