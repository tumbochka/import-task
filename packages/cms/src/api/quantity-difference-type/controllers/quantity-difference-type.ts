/**
 * quantity-difference-type controller
 */
import { factories } from '@strapi/strapi';

export default factories.createCoreController(
  'api::quantity-difference-type.quantity-difference-type',
);
