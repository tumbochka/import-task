/**
 * enrolled-lead-condition controller
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreController(
  'api::enrolled-lead-condition.enrolled-lead-condition',
);
